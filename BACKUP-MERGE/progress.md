Original prompt: create a modernized clone of the arcade game Discs of Tron. This should be a Virtual Reality webxr game that supports VR mode, AR mode, and normal mode. We can call it Digi World. It should be 3D like many of the popular 3D games that are out there with humanistic like enemies, and a style very alike to the modern Tron movies. It should be an updated spin on the classic game. It should also include a secondary game modeling the light bikes where the player rides his bike and tries to destroy enemies with the light wall that comes from the bike, as they try the same to the player. The game is first person where the player is in the game and throwing/dodging discs and jumping between circular platforms or driving the bike. In AR mode, the game uses pre-set room setups and brings the enemies into the space where discs systematically break apart the space and revealing the digital world in the breaks. Award winning graphics and designs and emmersive experience.

## Build log

- 2026-07-11: Started from an empty Git repository. Chose Vite + Three.js with one renderer/canvas and a shared deterministic gameplay core.
- 2026-07-11: Visual direction established as an original "fractured digital cathedral" with obsidian ceramic forms, cyan/white player energy, coral adversaries, ultraviolet depth, faceted armor, and spatial UI.
- 2026-07-11: Added a cinematic responsive deployment menu, HUD, pause/results overlays, desktop/VR/AR mode selection, and three AR room presets.
- 2026-07-11: Implemented the Shard Arena vertical slice: first-person movement, mouse aim, jumping/dashing, charge/throw/recall, guard reflection, platform physics, telegraphed enemy attacks, three Sentinel roles plus a boss wave, scoring, health, particles, and room-fracture panels.
- 2026-07-11: Implemented Lightline Pursuit: cockpit view, grid movement/turning, boost/brake, toggleable light wall, disruption pulse, three tactical enemy riders, trail decay, collisions, armor respawn, score, minimap, and arena compression.
- 2026-07-11: Added real WebXR session negotiation for immersive-vr/immersive-ar, controller trigger/grip actions, haptics, AR transparency, graceful desktop preview fallback, deterministic time stepping, and text state output.
- 2026-07-11: Production build passes with Vite 7.3.6 and Three.js 0.180.x; browser gameplay QA is next.
- 2026-07-11: Playwright menu and arena passes render correctly at 1280×720 with no console/page errors; state output confirmed aiming, two successful hits, combo/score updates, jump traversal, active hostile projectiles, and disc return. Tightened the expanded AR configuration so launch remains visible on short viewports.
- 2026-07-11: AR desktop-preview QA confirmed the requested AR presentation fallback, two sequential reality fractures, central panel removal, portal reveal, a Sentinel elimination, and intact state reporting. Bike QA confirmed snap turning, boosting, live trails, all three AI riders, boundary collision, armor loss, and respawn staging. Reduced near-camera bloom and additive trail energy after visual inspection; deterministic tests now suppress real-time simulation while `advanceTime()` is driving frames.
- 2026-07-11: Added project documentation covering both loops, controls, local/build commands, WebXR secure-context requirements, AR capability fallbacks, deterministic hooks, and the remaining hardware-testing boundary.
- 2026-07-11: Pause-state QA confirmed keyboard pausing freezes simulation, releases pointer lock, exposes accessible resume/restart/exit actions, and preserves full game state. The decorative boot overlay no longer intercepts early launch input on slower GPU/software-rendering devices.
- 2026-07-11: Corrected the shared yaw convention so arena locomotion and the light-runner cockpit now move/look in the same world direction after turns. Survival score now accumulates frame-rate-independently, trail decay preserves the polished opacity, and desktop pointer lock is requested directly from the launch gesture.
- 2026-07-11: Added original generated obsidian-cathedral and armor-surface assets, rebalanced exposure/bloom for readable black materials, and added disc trails, impact shockwaves, animated environmental lighting, and motion treatment.
- 2026-07-11: Replaced the visible arena fallback with a normalized skinned digital-human Sentinel sourced from the official Three.js examples. The combatants now use blended idle/run clips, full sprint strides, progressive yaw, procedural upper-body wind-up/follow-through, a hand-mounted disc, hand-origin projectile release, hit response, and proper skeleton cleanup.
- 2026-07-11: Rebuilt the light runners as sculpted PBR motorcycles with monocoque shells, curved fairings, carbon keels, separated wheels/rims/brakes/hubs, forks, swingarms, windscreens, saddles, energy veins, contact shadows, and detailed seated silhouettes.
- 2026-07-11: Rebuilt the first-person bike cockpit with a curved windscreen, tubular frame, articulated swept handlebars, grips/levers, gloved forearms, live instruments, suspension movement, progressive camera yaw/lean, steering, wheel spin, and restrained afterimage motion.
- 2026-07-11: Visual QA captures confirm the skinned Sentinel at idle and in a planted sprint (`test-results/skinned-sentinel-fixed`, `test-results/skinned-sentinel-run`) plus the redesigned cockpit, nearby exterior runners, and mid-turn lean (`test-results/sculpted-bike-cockpit`, `test-results/sculpted-bike-turn`). The production build remains clean apart from Vite's advisory large-chunk warning.
- 2026-07-12: Recovered `progress.md` and the three Digi World runtime assets from known-good commit `7c4627e` after an accidental cross-project commit/reset removed the complete `public/` tree. The enhanced source code had survived unchanged; the missing Sentinel GLB was forcing the old procedural character fallback, while the missing cathedral and armor textures degraded the board and materials. PaGuire/VECTOR PROTOCOL files were intentionally not restored into this repository.
- 2026-07-12: Post-recovery production build passes, and the browser smoke test reaches active Shard Arena gameplay with both opponents reported as `skinned_digital_human`; the restored cathedral and articulated Sentinel silhouettes are visible with no app console/page errors.
- 2026-07-12: Lightline recovery smoke reaches active gameplay with all three runners, the sculpted exterior bikes, detailed curved cockpit, live gauges, restored materials, and upper-right minimap visible; state and console checks are clean.
- 2026-07-12: Recovered the later Vector Protocol snapshot from commit `5e27280` into `OTHERPROJECT/recovered-vector-protocol` as an isolated, runnable behavior reference. Its static-server smoke test reaches the Vector Protocol hub with no errors. This preserves the later frisbee/ricochet, room-scale AR breach, expanded Lightfield, lean/boost/emergency-stop, instructions, music, and rename work without replacing Digi World's high-detail renderer.
- 2026-07-12: Audited `/Users/crmcguire74/Documents/Claude CoWork/Paguire/site`; its Git worktree and source are clean and contain no game code. The apparent PaGuire run failure came from running npm one directory too high, while the mistaken game work exists only in PaGuire's `test-artifacts` output.
- 2026-07-12: Consolidated the recovered Vector Protocol behavior into the restored high-detail Three.js renderer: horizontal frisbee flight, visible disc trails and room/platform ricochets; room-scale AR breaches; a 163.5 m Lightline arena with an eight-second safe vector; lean/stick steering, trigger boost, braking, three emergency stops, persistent dashboard height, instructions, music, and the Vector Protocol identity.
- 2026-07-12: Reworked Lightline visual hierarchy after gameplay review. Removed full-frame afterimage ghosting, reduced additive wall/streak/light flashes, replaced the ambiguous perimeter with a persistent square architectural boundary, added rival role markers, enlarged the square tactical map, and retained the detailed motorcycle/cockpit. VR now uses one head-locked upper-right tactical board, while the dashboard counter-rotates to stay level as the chassis leans. Browser captures `test-results/vector-bike-locked-map` and `test-results/vector-bike-level-dash-turn` confirm all three runners, a completed turn, the safe vector, 163.5 m arena, three stops, level-panel state, locked map state, and no page/console errors.
- 2026-07-12: Reworked AR Discs so the physical room is the environment rather than importing the cathedral arena. Immersive AR now accepts and transforms a WebXR bounded-floor polygon after hit-test placement, then uses it for player support, enemy spawn/roaming, containment, and disc banks. Unavailable/invalid room bounds use an explicitly labeled size fallback. Calibration surfaces are faint and the digital cathedral appears only through localized wall/floor breaches. Browser capture `test-results/vector-ar-roomspace-final` confirms zero platforms, room-scale state, articulated Sentinels, no imported arena, and no page/console errors; live bounded-floor alignment still requires compatible hardware.
- 2026-07-12: Final production build passes under the renamed `vector-protocol` package. The only build note is Vite's existing advisory that the main WebGL bundle exceeds 500 kB.
- 2026-07-12: Fixed unintended Lightline VR rocking. Head lean previously activated after roughly 2.4 cm, two-grip lean had no neutral hysteresis, and straight driving still applied sinusoidal lateral camera bob. XR lean now uses smoothed 6.5/3.5 cm head and 5.5/3 cm controller enter/exit thresholds, 100 ms sustained intent, neutral recentering, exact zero roll/input, and a one-turn-until-neutral re-arm latch. Synthetic lateral motion is zero and immersive VR no longer adds suspension motion to head tracking. `test-results/vector-bike-straight-stability-final` proves a no-input run stays at cell X 0 with north heading, steering input/charge/roll 0, and no browser errors; `test-results/vector-bike-intentional-turn-regression` confirms an intentional turn still completes.
- 2026-07-12: Rebuilt AR room placement as an explicit setup flow. Arena actors and simulation remain hidden/frozen until the user confirms a valid cyan floor marker by trigger, Enter/Space, touch, or the placement button. The game prefers bounded-floor, rejects wall/steep hit poses below 0.94 world-up alignment, strips pitch/roll from anchors, falls back to local-floor/preset size, increases actor wall margins, and reports placement/upright state. `test-results/vector-ar-placement-pending` and `test-results/vector-ar-placement-confirmed` verify gating, confirmation, upright-dot 1.0 actors, and no browser errors.
- 2026-07-12: Replaced the AR breach's static cathedral image with a persistent opaque-edged 3D neon tunnel. Each opening has real depth, nine moving rings, a rotating local-axis lattice, helixes, particles, fractured shards, and per-frame motion; the hidden full-wall portal planes were removed. `test-results/vector-ar-animated-breach-3` confirms two wall openings with 3.02–3.26 m tunnel depth, live motion phases, nine moving rings each, `staticPlaneFallback: false`, visible depth, and no browser errors.
- 2026-07-12: The VR head-locked tactical map now renders the live light-wall network: every rider's trail draws as a continuous colored path in that rider's hue, fading on the same 8-to-12 second decay curve as the 3D walls, beneath the existing rider position/heading markers. Trail segments store their cell endpoints at creation so the 0.12 s map repaint stays allocation-light.
- 2026-07-12: Energized both environments toward the movie's living-grid feel. The shared digital-cathedral backdrop gained 120 rising data streams, four energy pulses circulating the circuit rings, sixteen flickering horizon data spires, and slow vertical breathing on the rings. Lightline's floor grid, outer grid, floor emissive, boundary rails, seams, supports, and wall emissive all pulse on offset phases; Shard Arena's void grid pulses while the 22 obsidian columns drift, rotate, and pulse their emissive individually. The data-stream respawn is deterministic (no Math.random) to protect the deterministic test hooks. Production build passes with only the pre-existing chunk-size advisory; a Node harness verified ring bob, arc rotation/opacity, rain wrap/buffer writes, and beam flicker ranges over 180 simulated frames.
- 2026-07-12: Brought light-wall paths to the desktop/AR HUD minimap, not only the VR tactical board. BikeMode now streams trail segments (owner color, age, cell endpoints) into `updateMinimap`, and DigiWorld lazily inserts a 256px `minimap__trails` canvas beneath the rider dots using the identical 50% + (cell/bounds) x 43% mapping, with the same 8-to-12 s fade as the 3D walls; `clearMinimap` wipes it on exit. A Node harness against the extracted draw path confirmed two live segments stroke, the aged-out segment is skipped, pixel mapping matches the dot formula, and alpha resets. Rebuilt `dist/` (new hashed index bundles + index.html); the stale pre-change bundles remain on disk because the workspace disallows deletes, but nothing references them.
- 2026-07-12: Gameplay overhaul pass. (1) Frisbee discs: the trigger now holds a visible disc on the XR controller and release throws it; release velocity comes from the controller's sampled swing (12-sample ring, 130 ms window, 0.72 swing / 0.28 aim blend, y clamped for flat flight, speed 13-30 mapping onto damage). Slow releases fall back to aim throws, and desktop keeps the charge mechanic. (2) Analog arc steering: the player cycle now rides in continuous space with a real heading; lean or small stick deflection steers proportionally with a quadratic response (max 2.6 rad/s), keyboard/tap/large-flick turns execute as fast 90-degree heading bursts, and the AI riders remain grid-based with both rasterizing into the shared occupancy cells. The old lean-charge snap-turn latch was removed. (3) Cycle crashes are full derezz events: 26-34 tumbling glowing voxels with gravity and floor bounce, dual particle bursts, stacked shockwaves, a stretching additive energy column, a point-light flash, and heavier haptics. (4) AR Discs prefers a saved custom room over presets: WebXR bounded-floor captures are persisted to localStorage, restored automatically next session, and players can trace their own room corners (R key or 2.2 s grip hold, trigger per corner, grip/R to commit, 1.5 m minimum, shoelace-validated polygon) which reuses the existing polygon containment. (5) Both games gained a grid-city horizon: night-storm gradient sky dome, 150-tower instanced lit skyline in one draw call, additive horizon energy glow, and a slow-drifting cloud deck, all fog-exempt and VR-cheap. Build passes; a 14-assertion Node harness verified straight-line zero drift, quadratic lean response, wall/boundary crashes with owner attribution, tap-turn bursts, swing detection and slow-release fallback, and room-trace geometry.

- 2026-07-17: Merge + optimize pass — brought the preferred build's campaign/gameplay systems onto this project's high-detail renderer and retimed everything to a TRON: Legacy Grid aesthetic. (1) Build: added `vite.config.js` (disabled the modulepreload polyfill that broke Rollup 4.62, split the three.js bundle) and repaired a corrupt esbuild install (x64 host had only the arm64 native binary — installed `@esbuild/darwin-x64`). Added `src/game/store.js` (mulberry32 seeded RNG + localStorage `Store`) as a deterministic backbone. (2) Disc Arena is now an authored campaign, not a wave survival: a program ladder BIT-3 → VANTA → SENTINEL-9 across PROVING RING / SHARD SPIRE / CORE VAULT, best-of-3 rounds, three integrity pips per combatant, arenas that shrink and reconfigure each round (single ring → 3 pads + floating shards → 5-pad cross + central pylon) with real void-death falling, per-program AI archetypes from one parameter table (windup/feint/guard/dodge/aim/bank), reactive dodge & guard-deflect, disc banks off shards/pylon, and `Store.discWins` progression. Kept the skinned Sentinel GLB (now orange-tinted per program) and the VR swing-throw. (3) Lightline Pursuit gained the campaign layer: three speed tiers (persisted to `Store.cycleTier`), best-of-3 rounds with a countdown intro and result pause, alternating readable formation starts, and graduated cut-off AI aggression — the existing bikes, cockpit, analog steering, emergency stops, and detonation FX are unchanged. (4) AR floor-break: a disc striking a real wall now also cracks the floor open beneath it — the floor breaks apart into tumbling neon-edged shards revealing the 3D neon tunnel of the digital world; falling into a floor breach costs a life, a disc that knocks a fighter through an opening costs two, and the program roams the mapped room footprint. (5) Aesthetic: palette moved to clean cyan player / orange adversaries / electric-blue Grid accents on glass-black; bloom restrained (0.42) and exposure lowered so neon reads as crisp lines; HUD shows the active program, ROUND n/3, integrity pips, and match score; the menu gained a campaign-progress readout with a Reset control. Production build passes; `advanceTime()`/`render_game_to_text()` headless checks confirm the disc round/pip/ladder transitions, ricochet banks, guard/void-death, cycle tiers/grace/best-of-3, and the AR wall→floor breach with pip loss; 15 s stability sims of all three modes run with no console errors.

- 2026-07-17: Feedback pass. (1) Disc arenas now put the player and program on SEPARATE facing platforms across an uncrossable void gap (real Discs of Tron) at every tier, not a shared ring. (2) Web/desktop robustness: `startGame()` clears `manualControl` so the live render loop always resumes after deterministic test stepping; verified real DOM keyboard/mouse play (move, throw across the gap to hit, guard). (3) Light cycle: removed the round-start freeze (the bike now moves immediately like the original, with only the 8s safe-vector ramp — this was the "shaking in place"), restored the original closer/spread rival spawn so enemies are visible and cross the player's path, and stripped the city skyline + cathedral backdrop for a clean Tron-arcade grid field. (4) VR HUD comfort: the status panel is now a rigid child of the camera fixed in the upper-left (no more per-frame reposition swim), and the tactical map is pushed farther out and enlarged in the upper-right so it reads at a comfortable focal distance. (5) Escalation: the player carries more discs on higher programs (2 → 2 → 3 on SENTINEL-9). Build passes; headless checks confirm separated platforms + cross-gap hits, immediate cycle movement with readable rivals and forming light-walls, and the per-tier disc count.

- 2026-07-17: Disc-game aesthetic pass toward the modern TRON Legacy world. (1) Discs redesigned as proper identity discs — a sleek near-black glass body with a single bright glowing rim (blooms), concentric white ring detail on both faces, and a luminous hub — replacing the textured "frisbee." (2) Disc-arena environment cleaned to a pure neon Grid: the city skyline and cathedral panorama are hidden, the floating obsidian octahedra removed, and the void replaced with a bright cyan grid abyss (near grid + vast faint outer grid + glass floor + additive cyan horizon band) under a deep-black sky, so the concentric-ring platforms float over a Grid horizon. Player pads read cyan, program pads orange. Build passes; no console errors.

## Current TODO

- On-headset validation still required (no headset available in the build environment): WebXR VR/AR entry, controller swing-throw comfort, guardian/bounded-floor alignment, AR passthrough plane detection, and cycle lean calibration. All XR code paths are wired and desktop-previewed but hardware-unverified.
- Profile the high-detail scene on target standalone headsets and, if needed, add model LODs for lower-end devices (the three.js bundle is now split into its own chunk).
- Playtest campaign difficulty/pacing on real input (feint read windows, pad-jump gaps per round, cycle cut-off aggression at tier 3) and tune the `AI`/`TIER_*` tables in `ArenaMode.js` / `BikeMode.js`.
- Keep Vector Protocol and PaGuire in separate repositories and verify the working directory before future prompts or Git operations.
